package p2p

// Peer: an interface representing the remote node.
type Peer interface {
	//
}

// Transport handles the communication between the nodes in the network: TCP, UDP, WEBSocket
type Transport interface {
	//
}
